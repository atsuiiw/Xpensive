# API base URL used in production builds (npm run build / deploy).
# Point this at the deployed backend. Override at deploy time if the host
# injects its own API_URI. Use a trailing-slash-free, fully qualified URL,
# e.g. https://api.example.com
API_URI=
